# See http://help.github.com/ignore-files/ for more about ignoring files.

# API Keys
secrets.json
RangerTrak-secrets.json
# NOTE: /src/index.html was listed here, but the file is tracked - and gitignore rules
# do not apply to tracked files, so the entry was inert and gave false protection.
# Angular requires index.html to be tracked; the real rule is simply: no keys in it.

# personnel info
/src/assets/imgs/REW
/src/assets/data/ranger*.json

# The internal planning/roadmap doc (unremediated security findings, named agency
# contacts, commercial strategy) now lives outside the repo entirely, one directory up,
# at ../PRIVATE-Roadmap.md - not relying on a gitignore rule to keep it out of the
# public repo.

# compiled output
/dist
/tmp
/out-tsc

# Generated API docs (`npm run compodoc`). Was tracked - 181 files, 8.2 MB - and went
# stale enough to be a liability: it still described AppModule and LazyModule long after
# both were deleted, and it republished a roster of real names and a key-shaped string
# that had already been removed from src/. Regenerate on demand; don't commit it.
/documentation
# Only exists if Bazel was run
/bazel-out

# dependencies
/node_modules

# Old code
/archived
/non-distributed

#Test and non-project stuff
/junk

# profiling files
chrome-profiler-events*.json

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# misc
/docs
/src-extra
/.angular/cache
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

# Firebase
.firebase
*-debug.log
.runtimeconfig.json
.env
node_modules
src/app/shared/policies.json

# Cloudflare Workers - local wrangler state and local secret overrides
.wrangler
.dev.vars*
