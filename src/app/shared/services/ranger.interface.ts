export interface RangerType {
  callsign: string
  fullName: string
  phone: string
  address: string
  image: string
  rew: string   // WA's Registered Emergency Worker number
  team: string
  role: string
  note: string
}
